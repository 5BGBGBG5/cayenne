# ─── Supabase (AiEO project: zqvyaxexfbgyvebfnudz) ─────────────────────────
# Cayenne's reddit_agent_* tables live in this project (shared with Saffron)
NEXT_PUBLIC_AIEO_SUPABASE_URL=
NEXT_PUBLIC_AIEO_SUPABASE_ANON_KEY=
AIEO_SUPABASE_SERVICE_KEY=              # Service role key for API routes (server-side)

# ─── Reddit OAuth2 ──────────────────────────────────────────────────────────
# Single app handles both organic data API and Ads API
REDDIT_APP_ID=                          # OAuth2 app ID (web app type)
REDDIT_APP_SECRET=                      # OAuth2 app secret
REDDIT_REDIRECT_URI=https://cayenne.inecta-salt.com/api/auth/reddit/callback

# ─── Anthropic ──────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=                       # Used for Layer 2 agent loop (Claude Sonnet 4.5)

# ─── Vercel Cron ────────────────────────────────────────────────────────────
CRON_SECRET=                            # Bearer token for cron endpoint auth
